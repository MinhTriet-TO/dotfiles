-- lua/custom/plugins/init.lua
--
-- Drop any extra plugin specs in this file (or create separate files in this
-- directory — lazy.nvim will pick them all up via the `{ import = "custom.plugins" }`
-- line in init.lua).
--
-- Example — adding a new plugin:
--
-- return {
--   {
--     "folke/todo-comments.nvim",
--     dependencies = { "nvim-lua/plenary.nvim" },
--     opts = {},
--   },
-- }
 
return {}
